[package]
name = "audioadapter-buffers"
version = "2.0.1"
edition = "2021"
rust-version = "1.74"
authors = ["HEnquist <henrik.enquist@gmail.com>"]
description = "A selection of wrappers and buffers for audio data"
license = "MIT"
repository = "https://github.com/HEnquist/audioadapter-buffers-rs"
keywords = ["audio", "buffer", "dsp", "sample", "pcm"]
categories = ["multimedia::audio"]
readme = "README.md"

[features]
default = ["std"]
std = ["audioadapter/std", "audioadapter-sample/std", "num-traits/std", "alloc"]
alloc = []

[dependencies]
num-traits.workspace = true
audioadapter = { version = "2.0.2", path = "../audioadapter", default-features = false }
audioadapter-sample = { version = "2.0.1", path = "../audioadapter-sample", default-features = false, features = ["audio"] }


[dev-dependencies]
criterion = "0.6"
audioadapter = { version = "2.0.2", path = "../audioadapter", features = ["test-utils"]}

[[bench]]
name = "iteration"
harness = false
